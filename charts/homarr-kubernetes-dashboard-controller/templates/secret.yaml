{{- if not .Values.homarr.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.homarr.apiKeySecret.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "homarr-dashboard-controller.labels" . | nindent 4 }}
type: Opaque
data: {}
{{- end }}
